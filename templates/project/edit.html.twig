{% extends 'base.html.twig' %}

{% block title %}To Do List - Modifier le projet{% endblock %}

{% block body %}
    <h1>Modifier le projet : {{ project.subject }}</h1>

    {{ include('project/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-warning" href="{{ path('project_index') }}">back to list</a>

    {{ include('project/_delete_form.html.twig') }}
{% endblock %}
